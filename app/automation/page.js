import AutomationClient from "./automation-client";

export const metadata = {
  title: "Automation | Oracle Terminal",
};

export default function AutomationPage() {
  return <AutomationClient />;
}
